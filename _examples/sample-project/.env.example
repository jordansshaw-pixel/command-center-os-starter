# Sample project env contract (no values).
# Copy to .env.local and fill locally; .env.local is gitignored.
# Restore encrypted values via _connectivity/scripts/restore-env-profile.ps1 -Profile sample

EXAMPLE_API_TOKEN=
EXAMPLE_BASE_URL=https://api.example.com
